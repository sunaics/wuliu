
/**
 * 获取本地图
 * @param folder // 文件夹名字 如 /static/images/home
 * @param fileName // 文件名 如 home-bg
 * @param format // 文件类型 如 png jpg
 * @returns {*|string}
 */
 

export  const urlTobase64 = (path, type='png') => {
    var url = path

    return url;
  };
  
   
